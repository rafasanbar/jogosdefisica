[project]
name = "lead-router"
version = "0.1.0"
description = "Routing engine for company leads by source and metadata"
authors = [{name = "AutoGenerated"}]
requires-python = ">=3.10"
readme = "README.md"

[project.optional-dependencies]
dev = ["pytest"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
